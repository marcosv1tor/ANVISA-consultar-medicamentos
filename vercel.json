{
  "installCommand": "if [ -d frontend ]; then cd frontend; fi; npm ci || npm install",
  "buildCommand": "if [ -d frontend ]; then cd frontend; fi; npm run vercel-build",
  "outputDirectory": "dist",
  "framework": null,
  "env": {
    "VITE_API_BASE_URL": "https://medware-backend.onrender.com/api"
  },
  "build": {
    "env": {
      "VITE_API_BASE_URL": "https://medware-backend.onrender.com/api"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}